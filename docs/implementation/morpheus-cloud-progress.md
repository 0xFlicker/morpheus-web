# Morpheus Cloud execution

- Goal created; Apple/Clerk handoff and both checkouts inspected.
- Web and native branches: `codex/morpheus-cloud`; pre-existing native cache/session/test edits are preserved separately.
- U1 deployed: separate production/development Neon and private report Blob stores; strict ownership, transactional save revisions, bounded retry receipts, reporting, deletion and daily retention. Development and production database/API/storage checks pass. Both initial schemas are installed in production.
- U2 committed: authored discovery catalog and observed achievement rules. Server comparisons exclude imports and require 20 other completed players; results remain explicitly unverified.
- U3 deployed: durable local checkpoints/outbox, identity archives, safe resume boundaries, preserved competing tab checkpoints, and quiet camera synchronization. Authenticated continuity and divergent gameplay passed between the in-app browser and Chrome. Public production anonymous gameplay, saves, reports, discovery and erasure preserving local saves also passed.
- U4 committed and pushed: shared native repository/coordinator/transport, scoped Clerk sessions, quiet save/resume, report uploads, discovery and durable deletion recovery. The final native boundary run passed 110 expanded cases; the Apple monitor/coordinator run passed 23. Unsigned compile and locally signed iOS Simulator builds pass. Anonymous native UI and live transport checks pass. Broader gameplay testing reproduces three existing media failures. The external Apple capability/profile-impact approval remains a prerequisite for affected provisioning and real Apple sign-in verification.
- U5 implementation ready: False Floor, LLC disclosures, public support form, native privacy controls/manifest and durable Apple account-deletion backend. Provider exchange/revocation and App Store release metadata remain pending.
- U6 deployed: admin-only report, session, save, and observed achievement browsing. Ordinary-user browser denial, anonymous API denial, authenticated/anonymous browser reports, actual production admin browsing, and authorized private attachment retrieval pass. Chrome blocked saving the downloaded JSON locally despite the server returning HTTP 200; manifest display and request status were independently verified.

## Current verification

The full web suite passed 424 tests across 78 files, with typecheck and the optimized build passing. This includes identity-transition checkpoint draining, camera acknowledgments, retained competing tab views, report wire-size/rate limits, durable Apple account deletion and flushing the exact paused runtime before cloud replacement. The authored catalog check covers 1,843 scenes. Native/web discovery groups match across six sections, 227 locations and 305 scene variants.

The pause-boundary fix passed independent source review and a real two-browser check after rebuilding/reloading both clients. From a shared scene 2090 checkpoint, the in-app browser reached scene 2180 while Chrome reached scene 2095. Chrome preserved both and offered the explicit choice; choosing the other device installed scene 2180 in the save slot and actual renderer, and it survived reload. This verifies concurrent divergent gameplay, not a browser network-offline simulation. The earlier ambiguous two-browser observation was not conclusively reproduced; a focused test separately confirmed the unflushed-runtime replacement race addressed by this fix.

Real development Neon checks verify concurrent writes, canonical idempotent retries after later camera/progress changes, retained checkpoints, and receipt quotas. The local API checks use real Neon/private Blob for ownership isolation, invalid-save rejection, private reports, comparison thresholds, and erasure. The maintenance runner verifies signed Clerk payloads with the actual verifier, linked-account erasure, and physical Blob cleanup. These are separate from actual provider delivery, authenticated browser, production, and native-device evidence.

The cloud preview at commit `b9354384` built successfully. Browser verification then found the native HTML game dialog obscured Clerk portals; a follow-up closes it before opening account controls and keeps sign-out in Morpheus. The optimized build passes after that correction.

Both Clerk deletion endpoints are configured, and each signing secret is installed in its matching Vercel environment. The production endpoint (`9l4gLW`) subscribes only to `user.deleted` at `https://www.soapbubble.xyz/api/webhooks/clerk`. Actual production Svix delivery passed with HTTP 200 (`msg_3IuKUL2ZlaEJMsVHc8O2CrwOPQF`), and its database deletion fence was verified. The displayed example target had no real player, Apple grant or deletion work before the test. Development delivery still needs an approved deployment-protection automation secret; automatic approval review rejected creating that access-control credential, and a scoped approval question is pending. No bypass was installed. See `docs/release/morpheus-cloud.md` for the operational contract.

The final web preview `dpl_9wePqrSzXGkYH1bJ6RJTtJKafKNG` built commit `f48b4878`. Public Contact submissions from localhost and the hosted preview returned receipts and were verified in development Neon under anonymous owners. The preview Contact-only identity has no sessions, saves or account association. Six unauthenticated admin API probes returned application HTTP 401 with `private, no-store`; a credential-free private Blob read returned 403.

A fresh production build of that commit (`dpl_BTwYAYF7gYhwHEAiS4rZDMrtVZk3`) finished while a new two-browser progress concern was being investigated. Production was immediately rolled back to `dpl_9vF985zK5U8xPBAzhtrHuV3FzMJ6`, and `www.soapbubble.xyz` was independently verified on that prior release. Production still had zero players, saves and reports after rollback. The development branch alias was explicitly restored to the original preview deployment; a cross-environment rebuild can otherwise reuse its source branch alias.

The corrected commit `3401eff0d4b9be50e669603d8f70f5ffffaf6a7f` is now live at `www.soapbubble.xyz` in production deployment `dpl_HRwZcXNyfyjbXVxgyVZuTcrvXfxs`. It was built with automatic custom-domain assignment disabled, verified using its deployment URL, and explicitly promoted only after checks passed. The canonical domain's assignment was independently verified. Matching preview `dpl_H6EeAW3UTitZaUjakLv2qghK8kWk` uses development resources, with the development branch alias explicitly assigned to it.

Production verification covered Privacy/Terms/Support, save registration/write/retry/resume, independent ownership, reports with private Blob diagnostics, six anonymous admin/attachment denials, and cleanup of only synthetic players. Real public browser play displayed 0.4% discovery; admin showed its anonymous session, save and “First discovery” achievement. Both signed-in support and anonymous in-game report rows were browsed by the existing production admin. Online erasure through the game removed the anonymous records while retaining the local scene 2000 checkpoint. The exact synthetic support record and otherwise empty cloud player were also removed, preserving the Clerk account. Production ended with zero player/save/report rows; private orphan attachments follow the disclosed daily cleanup after 24 hours.

The production retention endpoint returned 401 anonymously and 200 with its configured secret; all cleanup/pending-work counts were zero. This verifies a manual invocation of the scheduled route, not the next automatic scheduled run. The promoted deployment had zero error-level entries in the first inspected 30-minute release window. Web sign-out was also verified to remain in Morpheus, restore the device's anonymous catalog, and leave the other browser's account signed in.

The native compiled debug transport also passed an explicit real API run against localhost:3105: 17 requests covered independent anonymous identities, durable exact retry, fresh-repository/runtime resume, camera-only revision stability, a real divergent-write conflict retained across reopening, explicit resolution, ownership denial, and session recording. Only the two newly created synthetic players were erased. This is compiled native transport/storage/runtime evidence, not authenticated-device or Apple-provider verification. Native commit `7c0e1bc` also prevents a stale runtime's camera-only movement from becoming a false conflict after remote progress/deletion; 30 test methods (33 expanded cases), independent review, and the repeated live transport run passed.

A separate disposable iOS 26.5 Simulator now provides native UI evidence: anonymous start without a sign-in prompt, authored progress to scene 2050, local persistence through process termination/relaunch, the False Floor, LLC privacy disclosure, and an anonymous Bug Snapshot submission returning “Report received.” The Simulator build requires local ad-hoc signing because Clerk's keychain initialization cannot run in an unsigned app. Both unsigned compile verification and the runnable locally signed build pass; neither changes Apple provisioning profiles or verifies real Apple sign-in.

Clerk Native API is enabled in both environments, and both now register the verified `9X3DZHNHU6.xyz.soapbubble.morpheus` app with `xyz.soapbubble.morpheus://callback`. Separate Apple account-service encryption keys are installed in production and development/preview. The Apple portal still requires user sign-in and explicit approval of the provisioning-profile impact. No Apple signing key or production OAuth provider has been configured yet.

The Apple backend's final review corrected a late exchange/deletion race with an admitted reservation that remains attached to its receipt after target erasure. Focused tests and isolated PostgreSQL regressions verify late completion, concurrent revocation, abandoned reservations and sticky manual-revocation outcomes. Provider calls were mocked for this evidence.

## Initial evidence

Both platforms already use `morpheus-living-save-session`, schema 1, game-data version 1. Each has three save slots. Web uses IndexedDB and authored state/scene validation; native JSON encoding matches the same fields. Web `/admin` already checks configured Clerk user ID plus verified admin email. No cloud API routes or native Clerk integration were present at inspection.

The linked Vercel project is `morpheus-web-www` (`prj_Ck4KfsUOD3ECmJMr9JtfhGNHFSND`), team `team_Eam5iv5Hn4S5k8mGzMlZRSpH`.
